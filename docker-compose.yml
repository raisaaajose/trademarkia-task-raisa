version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - ENV=production
    # This allows to see logs in real-time
    stdin_open: true
    tty: true